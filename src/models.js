const config = require('../config');

// const newMongoose = require('./common/mongoose');

// const mongooseInstance = newMongoose(config.mongo);


module.exports = {
  // db connectors
  // mongooseInstance,

  // models
};
